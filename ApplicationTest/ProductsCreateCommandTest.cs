namespace ApplicationTest
{

    public class ProductsCreateCommandTest
    {
        //[Fact]
        //public async Task ProductsCreate()
        //{
        //    var productRepository = new Mock<ProductRepository>();

        //    var handler = new CreateProductCommandHandler(productRepository.Object);

        //    var id = await handler.Handle(new CreateProductCommand(), new System.Threading.CancellationToken());

        //    Assert.Equal(long);

        //}
    }
}
